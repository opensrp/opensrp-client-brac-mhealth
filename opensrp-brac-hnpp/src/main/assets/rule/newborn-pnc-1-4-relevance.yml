---
name: step1_weight_warning_less
description: weight_warning relevance
priority: 1
condition: "((!step1_weight.isEmpty() && step1_weight < 2500) || (!step1_birth_weight.isEmpty() && step1_birth_weight < 2500))"
actions:
  - "isRelevant = true"
---
name: step1_weight_warning_up
description: weight_warning relevance
priority: 1
condition: "(!step1_weight.isEmpty() && step1_weight > 4500) || (!step1_birth_weight.isEmpty() && step1_birth_weight > 4500)"
actions:
  - "isRelevant = true"
---
name: step1_is_risk
description: is_risk calculation
priority: 1
condition: "!step1_weight.isEmpty() &&step1_weight<2500"
actions:
  - "calculation = 1"
---
name: step1_is_risk_temparature
description: weight_warning relevance
priority: 1
condition: "!step1_body_temparature.isEmpty() &&(step1_body_temparature.equals('তাপমাত্রা বেশি/জ্বর -৩৭.৫oসে বা ৯৯.৫ ফারেনহাইট এর উপরে')||step1_body_temparature.equals('তাপমাত্রা ক়ম - ৩৬.৫o সে বা ৯৭.৭ ফারেনহাইট এর নিচে'))"
actions:
  - "isRelevant = true"
---
name: step1_temparature_warning
description: weight_warning relevance
priority: 1
condition: "!step1_body_temparature.isEmpty() && (step1_body_temparature.equals('তাপমাত্রা বেশি/জ্বর -৩৭.৫oসে বা ৯৯.৫ ফারেনহাইট এর উপরে')||step1_body_temparature.equals('তাপমাত্রা ক়ম - ৩৬.৫o সে বা ৯৭.৭ ফারেনহাইট এর নিচে'))"
actions:
  - "isRelevant = true"
---
name: step1_pre_term_warning
description: weight_warning relevance
priority: 1
condition: "!step1_birth_weight.isEmpty() && step1_birth_weight < 2500"
actions:
  - "isRelevant = true"
