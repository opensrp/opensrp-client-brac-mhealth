---
name: step1_age_calculated
description: Age calculated
priority: 1
condition: "true"
actions:
  - "calculation = ((step1_is_birthday_known.equals('না')||step1_is_birthday_known.equals('No')) && !step1_estimated_age.isEmpty()) ? step1_estimated_age : helper.getDifferenceDays(step1_dob) / 365.25"
---
name: step1_child_age_calculated
description: Age calculated
priority: 1
condition: "true"
actions:
  - "calculation = helper.getDifferenceDays(step1_dob) / 365.25"
---
name: step1_wra
description: wra calculation
priority: 1
condition: "step1_sex == 'Female' && (step1_age_calculated >= 15 && step1_age_calculated <= 49)"
actions:
  - "calculation = 1"
---
name: step1_is_risk
description: is_risk calculation
priority: 1
condition: "step1_birth_weight<2500 && step1_child_age_calculated<=3"
actions:
  - "calculation = 1"