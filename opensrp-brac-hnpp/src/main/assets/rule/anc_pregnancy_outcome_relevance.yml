---
name: step1_miscarriage_date
description: miscarriage_date
priority: 1
condition: "step1_preg_outcome == 'miscarriage'"
actions:
  - "isRelevant = true"
---
name: step1_no_children
description: no_children relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'born_alive_died' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_delivery_place
description: delivery_place relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'still_born'"
actions:
  - "isRelevant = true"
---
name: step1_delivery_place
description: delivery_place relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'still_born'"
actions:
  - "isRelevant = true"
---
name: step1_csba_trained
description: csba_trained relevance
priority: 1
condition: "step1_place_of_delivery == 'বাড়িতে' || step1_place_of_delivery == 'কমিউনিটি ক্লিনিক'"
actions:
  - "isRelevant = true"
---
name: step1_delivery_method
description: delivery_method relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'still_born'"
actions:
  - "isRelevant = true"
---
name: step1_delivery_complication
description: delivery_complication relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'still_born'"
actions:
  - "isRelevant = true"
---
name: step1_delivery_home
description: delivery_home relevance
priority: 1
condition: "step1_delivery_place == 'At home'"
actions:
  - "isRelevant = true"
---
name: step1_delivery_hf
description: delivery_hf relevance
priority: 1
condition: "step1_delivery_place =='At a health facility'"
actions:
  - "isRelevant = true"

---
name: step1_type_of_disability
description: type_of_disability relevance
priority: 1
condition: "step1_disabilities == 'Yes' && (step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem')"
actions:
  - "isRelevant = true"

---
name: step1_baby_label
description: delivery_place relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_first_name
description: first_name relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_gender
description: gender relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_birth_weight_taken
description: birth_weight_taken relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_chlorohexadin
description: chlorohexadin relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_breastfeeding_time
description: breastfeeding_time relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_head_body_covered
description: head_body_covered relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_birth_defect
description: birth_defect relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"
---
name: step1_breast_feeded
description: breast_feeded relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_unique_id
description: unique_id relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_surname
description: surname relevance
priority: 1
condition: "!step1_same_as_fam_name_chk.contains('same_as_fam_name') && (step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem')"
actions:
  - "isRelevant = true"

---
name: step1_same_as_fam_name_chk
description: same_as_fam_name relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_fam_name
description: fam_name relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_first_name
description: first_name relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_middle_name
description: middle_name relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_gender
description: gender relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_disabilities
description: disabilities relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_early_bf_1hr
description: early_bf_1hr relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"

---
name: step1_vacc_birth
description: vacc_birth relevance
priority: 1
condition: "step1_preg_outcome == 'born_alive' || step1_preg_outcome == 'alive_with_problem'"
actions:
  - "isRelevant = true"